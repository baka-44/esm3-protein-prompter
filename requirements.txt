# Core ML / ESM3
esm>=3.0.0
torch>=2.0.0

# ESM2 log-likelihood scoring (HuggingFace)
transformers>=4.40.0

# Anthropic Claude API (NL parser)
anthropic>=0.25.0

# Streamlit UI
streamlit>=1.35.0
py3Dmol>=2.0.0

# Bioinformatics
biopython>=1.83

# Data / utils
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Google OAuth2 (production auth on Cloud Run)
google-auth-oauthlib>=1.2.0
google-auth>=2.28.0

# RFdiffusion/MPNN backend (proteinredesign) — frontend job submission + dashboard
# Pinned deliberately: an unpinned rebuild silently moved these and job submission started
# failing with a Firestore 400. Bump them on purpose, not as a side effect of a rebuild.
google-cloud-storage==3.13.1
google-cloud-firestore==2.28.1
google-cloud-run==0.16.1
